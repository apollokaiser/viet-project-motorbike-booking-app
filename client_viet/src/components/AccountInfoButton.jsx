export default function AccountInfoButton({name,id}) {
   return (
    // Thêm router dẫn đến trang cá nhân
    <>
        <button>
            <span>Xin chào</span><span>{name}</span>
        </button>
    </>
   ) 
}